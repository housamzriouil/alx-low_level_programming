#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;

	n=0;
	for (n=0 ; n<10 ; n++)
	{
		printf ("%d, n);
	}

	return (0);
}
