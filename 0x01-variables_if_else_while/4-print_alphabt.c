#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
        char C;

        do {
		for (C = 'a' ; C <= 'z' ; C++)
        {
                putchar(C);
        } while (C!=e || C!=q) 
                putchar('\n');

        return (0);
}
