#include "main.h"
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
void print_alphabet(void);
{
	char C;

	for (C = 'a' ; C <= 'z' ; C++)
	{
		putchar(C);
	}
		putchar('\n');
		return (0);
}
