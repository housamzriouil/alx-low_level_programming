#include "main.h"
/**
 * _abs - a function that computes the absolute value of an integer
 * @n: number input
 */
int _abs(int)
{
	int n;

        if (n > 0)
        {
		return (n);
        } else if (n == 0)
        {
		return (0);
        }
        {
		return (-n);
        }
}
